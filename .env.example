# Local development (SQLite — works out of the box)
DATABASE_URL="file:./dev.db"

# Production (Supabase / Railway / Vercel Postgres)
# Change provider in prisma/schema.prisma to "postgresql" and run migrations.
# DATABASE_URL="postgresql://user:password@host:5432/pencil?sslmode=require"

# Public URL for share links (set in production)
# NEXT_PUBLIC_APP_URL="https://your-domain.com"
